#  install homebrew using Terminal on Mac
# /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
# then 
# brew install git 
# on the terminal
# close r and reopen again.

